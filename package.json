{
  "name": "loui-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --watch src --exec \"npx tailwindcss -i ./src/input.css -o ./dist/output.css && npx live-server ./dist\"",
    "build": "tailwindcss -i ./src/input.css -o ./dist/output.css --minify"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2",
    "nodemon": "^3.1.0",
    "tailwindcss": "^3.4.1"
  },
  "dependencies": {
    "watch": "^1.0.2"
  }
}
